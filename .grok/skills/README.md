# Project-scoped Grok skills

Đặt skill riêng repo tại:

```
.grok/skills/<skill-name>/SKILL.md
```

Skill ở đây chỉ có hiệu lực trong project này (không global user).

Xem hướng dẫn tạo skill: Grok `/create-skill` → scope **Project**.
